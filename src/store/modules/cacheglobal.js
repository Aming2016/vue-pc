import axios from "axios";
import * as types from "../mutation-types";

// state永久存储的个人数据
export const state = {
    list:"421341243213421",
    data:"fsafsdf"
};

// getters
export const getters = {
    list: state => state.list,
};

// mutations
export const mutations = {
};

// actions
export const actions = {
};
